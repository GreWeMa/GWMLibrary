VERSION 2.5.2
* Use `org.apache.commons.lang3.tuple` instead of implementing our own
* Rename `utils` package to `util`
* Rename `SuperObjectsService` and `SuperObjectsServiceImpl` to `SuperObjectService` and `dev.gwm.spongeplugin.library.util.service.SuperObjectServiceImpl` accordingly
* Move `SuperObjectService` and `dev.gwm.spongeplugin.library.util.service.SuperObjectServiceImpl` from `util` package to `util.service`
* Refactor `Version`

VERSION 2.5.1
* Use default type within category when creating a Super Object if it is not ambiguous
* Rework `SuperObjectsService` and `SuperObjectsServiceImpl`. Created Super Objects are now not required to have unique ids

VERSION 2.5
* Add `getMessage` to `GWMLibraryUtils`
* Now the plugin uses its own logger to log message about a plugin not having default translation asset

VERSION 2.4
* Remove update checker (thanks to Ore developers for their genius idea with authentication...)
* Update comments in the translations

VERSION 2.3
* Add `parseParticleEffect(ConfigurationNode)` and `parseFireworkEffect(ConfigurationNode)` methods to `GWMLibraryUtils`

VERSION 2.2
* All Super Objects are now required to have an id

VERSION 2.1
* Add `ColorHexSerializer`
* Refactor `SpongePlugin`
* Refactor `Language`

VERSION 2.0
* Release